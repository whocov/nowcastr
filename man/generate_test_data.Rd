% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{generate_test_data}
\alias{generate_test_data}
\title{Generate asymptotic test data for nowcating}
\usage{
generate_test_data(
  n_reportdates = 9,
  n_delays = 10,
  reportdate_from = "2025-02-01",
  delay_from = 0,
  time_units = "days",
  final_value = 100,
  c = 0.5,
  b = 0.9,
  remove_delay = FALSE
)
}
\arguments{
\item{n_reportdates}{Integer. Number of consecutive report dates to generate.}

\item{n_delays}{Integer. Number of delay values to generate.}

\item{reportdate_from}{Character or Date. Start report date (e.g. "2025-02-01").}

\item{delay_from}{Integer >= 0. Minimum delay value.}

\item{time_units}{Time units. Accepted values: ("auto", "secs", "mins", "hours", "days", "weeks").}

\item{final_value}{Numeric. Asymptotic target (a in the formula).}

\item{c}{Numeric (0-1). Baseline fraction of final_value added to the curve.}

\item{b}{Numeric. Rate constant controlling how fast y approaches a; larger b → faster approach.}

\item{remove_delay}{Logical. If TRUE, remove the delay column in the output.}
}
\value{
A tibble (long format) with columns date_report (Date), date_occurrence (Date), delay (integer, optional) and value (numeric).
}
\description{
Create synthetic long-format test data following an asymptotic delay curve
and constant final value. Formula:
\verb{value = final_value * (c + 1 * (1 - exp(-b * .data$delay))))}
}
\details{
With defaults this returns n_reportdates × (n_delays - delay_from + 1) rows (9 × 10 = 90).
reportdate_from may be provided as a Date or parsable character; lubridate units are used for stepping.
}
\examples{
generate_test_data()
generate_test_data(n_reportdates = 3, n_delays = 3)
generate_test_data(time_units = "weeks", remove_delay = TRUE)
}
